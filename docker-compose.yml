services:
  react-prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3001"
    environment:
      - NODE_ENV=production
    restart: always   # garante que reinicia até após reboot
