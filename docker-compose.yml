services:
  react-prod:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["npx", "serve", "-s", "build"]
    environment:
      - NODE_ENV=production
    restart: always   # garante que reinicia até após reboot
