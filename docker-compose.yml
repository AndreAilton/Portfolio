services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio_git:latest
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=${FRONTEND_INTERNAL_PORT} # Define a porta interna via variável
    networks:
      - app-network

networks:
  app-network:
    driver: bridge