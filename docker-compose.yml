services:
  react-prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    environment:
      - NODE_ENV=production
    restart: always   # garante que reinicia até após reboot
